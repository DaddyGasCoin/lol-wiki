extends layout

block content
  h1= title
  a.button.update.res.create.home.end(href='/') HOME
  ul
    each _class in class_names 
      li
       a(href=_class.url) #{_class.name}  


    else
      li There are no books.
  a.button.update.res.create(href='/class/create') Create new class